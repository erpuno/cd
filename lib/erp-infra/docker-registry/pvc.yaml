apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-data
  namespace: erp-core
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
