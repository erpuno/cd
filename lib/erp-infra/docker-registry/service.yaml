apiVersion: v1
kind: Service
metadata:
  name: docker-registry
  namespace: erp-infra
  labels:
    app: docker-registry
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 5000
    targetPort: 5000
  selector:
    app: docker-registry
