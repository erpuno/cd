apiVersion: apps/v1
kind: Deployment
metadata:
  name: nitro-portal
  namespace: erp-services
  labels:
    app: nitro-portal
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nitro-portal
  template:
    metadata:
      labels:
        app: nitro-portal
    spec:
      serviceAccountName: erp-services-sa
      containers:
      - name: nitro-portal
        image: erpuno/nitro-portal:2024.6.15
        ports:
        - name: http
          containerPort: 8510
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 2
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - nitro-portal
              topologyKey: kubernetes.io/hostname
