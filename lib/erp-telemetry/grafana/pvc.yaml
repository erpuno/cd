apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: erp-telemetry
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hostpath
  resources:
    requests:
      storage: 5Gi
